//Creating our own History Object for Programmatic Routing 
//if we need to access history object in our action creators 

import { createBrowserHistory } from "history";

export default createBrowserHistory();

